# Socket.io collaboration server
VITE_BACKEND_URL=http://localhost:3000

# Code runner — languages load from the public API by default.
# Running code needs a local Piston instance (public execute is restricted):
#   cd "zollab net" && docker compose up -d
#   VITE_PISTON_API_URL=http://localhost:2000/api/v2
